CREATE TABLE Item (
    id INTEGER NOT NULL PRIMARY KEY AUTOINCREMENT,
    title TEXT NOT NULL,
    created_at INTEGER NOT NULL
);

createItem:
INSERT INTO Item(title, created_at)
VALUES (?, ?);

getAllItems:
SELECT * FROM Item ORDER BY created_at DESC;

deleteItem:
DELETE FROM Item WHERE id = ?;